# Development: install deps only; source is mounted at runtime
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# Prisma generate will run on startup via compose command
COPY prisma ./prisma
